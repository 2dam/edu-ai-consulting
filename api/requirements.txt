fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.10
openai>=1.51
python-dotenv>=1.0
numpy>=2.1
# torch 는 선택적 의존성 — 관측치가 충분히 쌓이면(app/imputation.py 의
# VAE_MIN_OBSERVATIONS 이상) 설치해서 VAE 인페인팅 경로를 활성화.
# torch>=2.4
